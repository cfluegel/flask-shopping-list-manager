# =============================================================================
# Flask Grocery Shopping List - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust the values for your environment.
#
#   cp .env.example .env
#
# Lines starting with # are comments. Uncomment a line to activate it.
# =============================================================================

# -----------------------------------------------------------------------------
# Flask
# -----------------------------------------------------------------------------
# Configuration class (config.ProductionConfig for deployments)
FLASK_CONFIG=config.ProductionConfig

# Set to 1 only during development
FLASK_DEBUG=0

# -----------------------------------------------------------------------------
# Security  *** REQUIRED - change before first start ***
# -----------------------------------------------------------------------------
# Generate each key with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE-ME-generate-a-random-hex-string
JWT_SECRET_KEY=CHANGE-ME-generate-a-different-random-hex-string

# -----------------------------------------------------------------------------
# Database (PostgreSQL - used by Docker Compose)
# -----------------------------------------------------------------------------
# Credentials for the postgres container and the app's DATABASE_URL.
DB_USER=grocery_user
DB_PASSWORD=CHANGE-ME-use-a-strong-password
DB_NAME=grocery_db

# Override the full connection string if you use an external database.
# When running with Docker Compose this is built automatically; you do NOT
# need to set it unless you manage PostgreSQL yourself.
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# For local development without Docker you can use SQLite:
# DATABASE_URL=sqlite:///app.db

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins (no spaces).
# Use * only during development.
CORS_ORIGINS=https://your-domain.com

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------
# In Docker Compose this points to the redis service automatically.
# Override only if you run Redis elsewhere.
# RATELIMIT_STORAGE_URL=redis://custom-redis:6379/0

# -----------------------------------------------------------------------------
# Server / Gunicorn
# -----------------------------------------------------------------------------
# Host port that maps to the container's port 8000.
APP_PORT=8000

# Gunicorn tuning (sensible defaults for a small VPS)
GUNICORN_WORKERS=4
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=60

# -----------------------------------------------------------------------------
# Domain & Traefik (reverse proxy / HTTPS)
# -----------------------------------------------------------------------------
# Set DOMAIN to your FQDN to enable Traefik routing labels on the app service.
# Leave as localhost when running without Traefik.
DOMAIN=localhost

# Email for Let's Encrypt certificate notifications.
# Required when using the with-traefik profile.
ACME_EMAIL=you@example.com

# Enable the Traefik dashboard (true / false).
TRAEFIK_DASHBOARD=false

# Port for the Traefik dashboard (only used when TRAEFIK_DASHBOARD=true).
TRAEFIK_DASHBOARD_PORT=8080

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Application log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Production default in config.py is WARNING.
# LOG_LEVEL=WARNING

# -----------------------------------------------------------------------------
# Receipt Printer (ESC/POS network printer, optional)
# -----------------------------------------------------------------------------
# Uncomment and configure if you have a thermal receipt printer.
# PRINTER_ENABLED=false
# PRINTER_HOST=192.168.1.119
# PRINTER_PORT=9100
# PRINTER_PROTOCOL=network
# PRINTER_TIMEOUT=5
# PRINTER_WIDTH=80

# -----------------------------------------------------------------------------
# Sentry Error Tracking (optional)
# -----------------------------------------------------------------------------
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# -----------------------------------------------------------------------------
# Email / SMTP (optional, for future use)
# -----------------------------------------------------------------------------
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=noreply@example.com
# MAIL_PASSWORD=CHANGE-ME
# MAIL_DEFAULT_SENDER=noreply@example.com
